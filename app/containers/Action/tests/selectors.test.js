// import { fromJS } from 'immutable';
// import { makeSelectActionDomain } from '../selectors';

// const selector = makeSelectActionDomain();

describe('makeSelectActionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
