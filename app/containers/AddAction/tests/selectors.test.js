// import { fromJS } from 'immutable';
// import { makeSelectAddActionDomain } from '../selectors';

// const selector = makeSelectAddActionDomain();

describe('makeSelectAddActionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
