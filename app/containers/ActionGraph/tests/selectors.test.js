// import { fromJS } from 'immutable';
// import { makeSelectActionGraphDomain } from '../selectors';

// const selector = makeSelectActionGraphDomain();

describe('makeSelectActionGraphDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
